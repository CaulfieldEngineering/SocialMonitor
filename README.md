# SocialMonitor

Desktop app that monitors social media and forums for relevant posts with AI-powered relevance scoring and Windows desktop notifications.

## Quick Start

```bash
pip install -e .
python -m social_monitor
```

## Configuration

On first run, edit `%APPDATA%/SocialMonitor/config.yaml` or use the Settings dialog from the system tray icon.
